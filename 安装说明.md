# 智能盆栽IoT项目 - 快速安装指南

## 📦 环境要求

- Python 3.8 或更高版本
- macOS / Linux / Windows
- 网络连接（用于语音识别API）

## 🚀 快速安装

### 1. 创建虚拟环境（推荐）

```bash
# 使用conda（如果已安装）
conda create -n SmartPlanter python=3.11
conda activate SmartPlanter

# 或使用venv
python -m venv venv
source venv/bin/activate  # macOS/Linux
# venv\Scripts\activate  # Windows
```

### 2. 安装所有依赖

```bash
# 在项目根目录运行
pip install -r requirements.txt
```

### 3. 验证安装

```bash
python -c "import flask; import cv2; import mediapipe; import speech_recognition; print('✅ 所有依赖安装成功！')"
```

## 📚 各模块依赖说明

### Web服务器端（必需）
```bash
cd Web/
pip install -r requirements.txt
```

包含：
- Flask - Web框架
- SpeechRecognition - 语音识别
- opencv-python - 图像处理
- mediapipe - 动作识别
- numpy - 数值计算

### 本地测试模块（可选）

如果需要在本地运行语音或动作识别测试：

```bash
# 语音识别（需要本地麦克风）
cd Voice/
pip install SpeechRecognition PyAudio

# 动作识别（需要本地摄像头）
cd Gesture/
pip install opencv-python mediapipe numpy
```

## ⚙️ 硬件端依赖

硬件端（ESP32）使用MicroPython，不需要pip安装。所需的模块已经包含在代码中：
- `urequests` - HTTP请求（MicroPython内置）
- `ujson` - JSON处理（MicroPython内置）
- `machine` - 硬件控制（MicroPython内置）

## 🔧 常见问题

### Q: mediapipe版本不匹配？
A: 使用 `pip install mediapipe>=0.10.5` 安装最新兼容版本

### Q: opencv-python安装失败？
A: 尝试：
```bash
pip install opencv-python-headless>=4.8.0
```

### Q: macOS上PyAudio安装失败？
A: 先安装portaudio：
```bash
brew install portaudio
pip install pyaudio
```

### Q: Windows上安装错误？
A: 可能需要安装Visual C++ Build Tools
从微软官网下载安装

## 📝 依赖版本说明

- **Flask 2.3.0**: 稳定的Web框架版本
- **opencv-python ≥4.8.0**: 支持ARM架构（M1/M2 Mac）
- **mediapipe ≥0.10.5**: 最新稳定版，支持多平台
- **numpy ≥1.24.0**: 兼容Python 3.11+

## 🎯 最小安装（仅运行Web服务器）

如果只需要运行Web服务器端：

```bash
pip install Flask==2.3.0 SpeechRecognition==3.10.0 opencv-python mediapipe numpy
```

## ✅ 安装验证清单

运行以下命令验证各模块：

```bash
# 1. Flask
python -c "from flask import Flask; print('✅ Flask OK')"

# 2. 语音识别
python -c "import speech_recognition; print('✅ SpeechRecognition OK')"

# 3. OpenCV
python -c "import cv2; print('✅ OpenCV OK')"

# 4. MediaPipe
python -c "import mediapipe; print('✅ MediaPipe OK')"

# 5. NumPy
python -c "import numpy; print('✅ NumPy OK')"
```

全部显示 ✅ 即表示安装成功！

## 🚀 下一步

安装完成后，运行Web服务器：

```bash
cd Web/
python app.py
```

访问 http://localhost:8080 查看界面。
